import { Dancing_Script, Inter } from "next/font/google";

export const base = Inter({ subsets: ["latin"], variable: "--font-sans" });
export const brand = Dancing_Script({
  subsets: ["latin"],
  variable: "--font-brand",
});
